#include<iostream>
#include<vector>

class Solution{
    public:
        int maxValue(vector<int> weight,vector<int> value,int capcity){
            // init the dp array 
            vector<vector<int>> dp(value.size()+1,vector<int>(weight.size()+1,0));

        }
}
